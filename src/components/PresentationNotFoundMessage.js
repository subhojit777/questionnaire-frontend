import React, {Component} from "react";

class PresentationNotFoundMessage extends Component {
  render() {
    return (
      <div>Could not find a presentation.</div>
    );
  }
}

export default PresentationNotFoundMessage;
